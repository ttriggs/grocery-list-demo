This is a demo of a simple Sinatra-based web app that keeps track of a grocery list.This is a demo of a simple Sinatra-based web app that keeps track of a grocery list.
Items are read and stored in a postgresql database. Javascript is minimally used to check if the entry is blank or if it is already in the list.

